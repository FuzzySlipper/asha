// @generated by protocol-codegen — DO NOT EDIT.
//
// Source of truth: engine-rs/crates/protocol/*. Regenerate with:
//   cargo run -p protocol-codegen
//
// Manual edits will be overwritten and are rejected by CI
// (harness/ci/check-contracts.sh).

// Branded identifier for a discrete simulated entity (over a 64-bit integer).
export type EntityId = number & { readonly __brand: 'EntityId' };
export const entityId = (raw: number): EntityId => raw as EntityId;

// Branded identifier for an acting subject / authority (over a 64-bit integer).
export type SubjectId = number & { readonly __brand: 'SubjectId' };
export const subjectId = (raw: number): SubjectId => raw as SubjectId;

// Branded identifier for an ongoing process (over a 64-bit integer).
export type ProcessId = number & { readonly __brand: 'ProcessId' };
export const processId = (raw: number): ProcessId => raw as ProcessId;

// Branded identifier for a state-machine mode (over a 64-bit integer).
export type ModeId = number & { readonly __brand: 'ModeId' };
export const modeId = (raw: number): ModeId => raw as ModeId;

// Branded identifier for an event signal type (over a 64-bit integer).
export type SignalId = number & { readonly __brand: 'SignalId' };
export const signalId = (raw: number): SignalId => raw as SignalId;

// Branded identifier for a tag label (over a 64-bit integer).
export type TagId = number & { readonly __brand: 'TagId' };
export const tagId = (raw: number): TagId => raw as TagId;
