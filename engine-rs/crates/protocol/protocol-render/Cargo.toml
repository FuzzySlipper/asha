[package]
name    = "protocol-render"
version.workspace = true
edition.workspace = true

[lints]
workspace = true

[dependencies]
core-ids = { path = "../../foundation/core-ids" }
protocol-assets = { path = "../protocol-assets" }
serde = { version = "1", features = ["derive"] }
